# SIRAWAT × BALL — project notes

GitHub source of truth (Grok + Codex share this repo):

**https://github.com/ton36475-lgtm/driveto3dworld**

- Product: trilingual 3D creative atelier (EN/TH/简体中文) — salon gallery **and** driveable grounds.
- Auth/db: OFF.
- Do not replace this sandbox `AGENTS.md`. Codex reads `AGENTS.md` **in the GitHub repo**.
- After meaningful app edits, push to `main` (or open a `grok/*` branch + PR).
- Codex works on `codex/*` branches and PRs into `main`.
