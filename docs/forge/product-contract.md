# SIRAWAT × BALL implementation contract

## Outcome and source of truth
Extend the existing bilingual salon and driveable grounds, preserving routes, art direction and original source snapshots. `ton36475-lgtm/driveto3dworld` is canonical per `AGENTS.project.md`; `new3dport` supplies local operations additions; `3Dportfolio` is historical. Work occurs on `codex/agm-forge-production` with a pull request into main.

Actors: visitor, Ton, Ball, local operator, structural worker, polish worker, independent checker.

Journeys:
1. Home → 3D gallery or static fallback → concept detail → prepare/export brief → manually contact the owner.
2. Drive → start → keyboard/touch exploration → collect a study → inspect catalogue → reset local progress.
3. Local desk → validate/save inquiry → assign → prepare quote status → overdue/close → export/restore validated backup.
4. Forge → preset/theme/scene/density/motion/section selection → live preview → validated JSON export.
5. Blender structure → frozen manifest → isolated polish → structural comparison → GLB validation → optional model with geometry fallback.

## Boundaries
Auth/database remain OFF per repository contract. Browser data belongs to that browser, is not team-shared, and is not an autonomous background service. No real email, LINE, Facebook publishing, payment, telemetry, Mac control, or MCP connection is claimed. No PDF financials, identity copies, addresses or artist-sensitive data enter the repository or public bundle. Supplied business plans are reported plans, not proof of delivery. Portfolio, hotel business and solar domain remain separate.

Existing work images/records are preserved for concept browsing. Case studies are labeled unverified and completion claims are suppressed. No added credentials, network proxy or quota bypass. Model/provider names are role suggestions only; current availability/entitlements and quota savings have not been measured.

## Acceptance
- Existing app stack and EN/TH retained; no fabricated delivery or analytics success.
- Reproducible locked install, safe startup, explicit readiness failure, portable clone paths.
- Config changes reflected in preview/export; invalid/extraneous data rejected.
- Build/typecheck, behavior tests, HTML route checks and browser checks on home/gallery/drive/contact/ops/forge/profiles.
- Responsive targets 360, 390, 768, 1440; keyboard navigation; reduced motion; touch controls; WebGL fallback; hidden tab suspension.
- Independent review and evidence receipt; changed receipt/artifact must fail SHA-256 verification.
- Repository-owned source/GLB outputs traceable; existing assets are not newly relicensed.

## Rollback and stop conditions
Rollback pointer: canonical base `734cbb8e7fc3e89e251adbd7fd3b7f9eaf7ca46d`. Do not merge/deploy while acceptance failures remain. Preserve base and branch; rollback by reverting the reviewed PR. Stop on unexpected credentials, shared-file collision, corrupt data, unknown asset license, structural drift, unsupported Blender/runtime version, expired work authority or unexpected external effects. Pipeline does not start a paid model request or remote worker automatically.

## Exactly one next safe packet
`OWNER-HOST-BLENDER-VERIFY-001`: after reviewing this branch, run the documented doctor and pinned Blender workflow on the already-authorized Mac mini workspace; verify its files and rendered views with an independent checker. Bind an actual host/MCP connection before executing there. This packet does not authorize public deployment or messaging.
