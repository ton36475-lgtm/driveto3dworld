Drop Draco-compressed .glb files here to replace the procedural zone pieces:

- arch.glb
- char.glb
- veh.glb
- prod.glb

The scene currently renders authored Three.js geometry as the fallback when these files are absent.
